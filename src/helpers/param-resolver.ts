// // Turn an array of params in the shape "key=value" into an object

// export default function parseParams(params) {
//   const result = {};
//   params?.forEach((param) => {
//     const [key, value] = param.split('=');
//     result[key] = value;
//   });
//   return result;
// }
